name: Delivery Scheduler

on:
  push:
    branches: [master]
    paths:
      - "deliveryscheduler/**"
      - "shared/**"
      - ".golangci.yml"
      - ".github/workflows/deliveryscheduler.yml"
  pull_request:
    branches: [master]
    paths:
      - "deliveryscheduler/**"
      - "shared/**"
      - ".golangci.yml"
      - ".github/workflows/deliveryscheduler.yml"

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: deliveryscheduler/go.mod
          cache: false

      - uses: actions/cache@v4
        with:
          path: |
            ~/go/pkg/mod
            ~/.cache/go-build
          key: go-deliveryscheduler-${{ runner.os }}-${{ hashFiles('deliveryscheduler/go.sum', 'shared/go.sum') }}
          restore-keys: go-deliveryscheduler-${{ runner.os }}-

      - run: make install-tools
      - run: make lint-deliveryscheduler

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: deliveryscheduler/go.mod
          cache: false

      - uses: actions/cache@v4
        with:
          path: |
            ~/go/pkg/mod
            ~/.cache/go-build
          key: go-deliveryscheduler-${{ runner.os }}-${{ hashFiles('deliveryscheduler/go.sum', 'shared/go.sum') }}
          restore-keys: go-deliveryscheduler-${{ runner.os }}-

      - run: make install-tools
      - run: make test-deliveryscheduler

  build:
    name: Build
    runs-on: ubuntu-latest
    needs: [lint, test]
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - run: make build-deliveryscheduler
